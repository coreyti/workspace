#!/usr/bin/env bash
set -euo pipefail

usage() {
  echo -e "\nUsage:

$(basename $0) <command>

commands:
  • -h | --help
"
}

main() {
  : ${WORKSPACE:?'unset - `direnv allow` first'}
  brew bundle --file ${WORKSPACE}/Brewfile
}

main "$@"
